import TopHeader from './TopHeader.js';
import MiddleHeader from './MiddleHeader.js';

const Header = `
    <header id="header" class="header">
        ${TopHeader}
        ${MiddleHeader}
    </header>
`;

export default Header;
